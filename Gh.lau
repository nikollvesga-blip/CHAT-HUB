-- Nikollvesga Menu KRNL Avanzado
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local RunService = game:GetService("RunService")
local workspace = game:GetService("Workspace")

-- Crear ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "NikollMenu"
screenGui.Parent = playerGui

-- Función para crear botones
local function crearBoton(nombre, posicion, tamaño, callback)
    local button = Instance.new("TextButton")
    button.Text = nombre
    button.Size = UDim2.new(0, tamaño.X, 0, tamaño.Y)
    button.Position = UDim2.new(0, posicion.X, 0, posicion.Y)
    button.BackgroundColor3 = Color3.fromRGB(255, 215, 0) -- dorado
    button.TextColor3 = Color3.new(0,0,0)
    button.Parent = screenGui
    button.MouseButton1Click:Connect(callback)
end

-- 1️⃣ Espamear Zombies
crearBoton("Espamear Zombie 🧟", Vector2.new(50,50), Vector2.new(200,50), function()
    for i=1,10 do -- puedes cambiar el número de zombies
        local zombie = Instance.new("Part")
        zombie.Size = Vector3.new(2,5,1)
        zombie.BrickColor = BrickColor.new("Really red")
        zombie.Position = player.Character.HumanoidRootPart.Position + Vector3.new(math.random(-10,10),0,math.random(-10,10))
        zombie.Anchored = false
        zombie.Parent = workspace
    end
    print("🧟 Zombies espameados!")
end)

-- 2️⃣ Animación Zombie
crearBoton("Animación Zombie 🧟‍♂️", Vector2.new(50,120), Vector2.new(200,50), function()
    local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildWhichIsA("Animator")
        if not animator then
            animator = Instance.new("Animator")
            animator.Parent = humanoid
        end
        local anim = Instance.new("Animation")
        anim.AnimationId = "rbxassetid://507768133" -- Animación de zombie (ejemplo)
        local track = animator:LoadAnimation(anim)
        track:Play()
        print("🧟 Animación zombie activada!")
    end
end)

-- 3️⃣ Espamear bloque con imagen de ChatGPT
crearBoton("Bloque ChatGPT 🟩", Vector2.new(50,190), Vector2.new(200,50), function()
    for i=1,5 do -- puedes cambiar cantidad
        local part = Instance.new("
